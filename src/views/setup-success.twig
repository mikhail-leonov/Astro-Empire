{% extends 'layout.twig' %}
{% block content %}
<section class="card">
  <h2>✅ Setup complete</h2>
  <p class="muted">Your database is connected and initialized.</p>
  {% if warn %}<div class="flash flash-error">{{ warn }}</div>{% endif %}
  <a class="btn primary btn-block" href="/register">Create your commander account →</a>
</section>
{% endblock %}
